#include <stdio.h>
/**
 * main - main function
 * Description - prints number from 0-9
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
